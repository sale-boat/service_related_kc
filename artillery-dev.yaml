config:
  target: 'http://localhost:3007'
  phases:
    - duration: 120
      arrivalRate: 5
      rampTo: 300
    - duration: 300
      arrivalRate: 300
  # http:
  #   pool: 20
scenarios:
  - flow:
    - get:
        url: "/api/related/{{ $randomNumber(1, 10000000) }}"
